info:
  name: delete contact
  type: http
  seq: 5

http:
  method: POST
  url: |
    http://localhost:8080/api/contact.php
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
