info:
  name: logout
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/users/logout
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
