info:
  name: createUser
  type: http
  seq: 1

http:
  method: POST
  url: http://contacts.0sake.net/api/auth.php
  body:
    type: json
    data: |-
      {
        "login_type": "create",
        "username": "jake",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
