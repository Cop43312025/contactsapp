info:
  name: createUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/create
  body:
    type: json
    data: |-
      {
        "login_type": "create",
        "username": "jake",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
