info:
  name: view
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/contacts/view/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
