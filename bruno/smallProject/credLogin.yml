info:
  name: credLogin
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/users/login
  body:
    type: json
    data: |-
      {
        "username": "Jacket",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
