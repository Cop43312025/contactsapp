info:
  name: edit contact2
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/src/api/contact.php
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
