info:
  name: tokenLogin
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/users/login
  body:
    type: json
    data: |-
      {
                "login_type": "token"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
