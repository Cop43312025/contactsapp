info:
  name: credLogin
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/auth.php
  body:
    type: json
    data: |-
      {
        "login_type": "credential",
        "username": "jake",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
